# Data

Non-production sample datasets and test fixtures used during development (e.g. sample
product catalogs and demand history for exercising the pricing engine).

- Only small, shareable, non-sensitive samples belong here.
- Large or generated artifacts are git-ignored (see `.gitignore`); keep the folder with
  `.gitkeep`.
- Never commit production, customer, or personally identifiable data.
