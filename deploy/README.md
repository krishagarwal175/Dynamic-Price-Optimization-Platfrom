# Deploy

Deployment and infrastructure configuration: Dockerfiles, docker-compose for local
orchestration, and any infrastructure-as-code. Kept separate from application source so
runtime/packaging concerns never leak into `backend/` or `frontend/`.
